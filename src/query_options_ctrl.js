import _ from 'lodash';

export class ConditionalSwitchQueryOptionsCtrl {
  constructor($scope, $injector, datasourceSrv) {
    console.log(datasourceSrv);
  }
}

ConditionalSwitchQueryOptionsCtrl.templateUrl = './partials/query.options.html';
